flowchart TD
    A["User Function"] -->|codegen| B["NKI kernel<br/>(baseline)"]

    B --> C["Frontier"]

    subgraph search ["Search Loop (main thread)"]
        C -->|"sample (depth-uniform)"| D["Find opportunities<br/>(symbolic analysis)"]
        D --> E["Apply transform<br/>(peel-and-modify)"]
        E --> F["DCE<br/>(cleanup)"]
        F --> G["Deduplicate<br/>(hash NKI kernel)"]
        G -->|new| C
        G -->|duplicate| H["discard"]
    end

    C -->|"submit unique variants"| I

    subgraph pool ["Worker Pool (parallel)"]
        I["Compile<br/>(neuronxcc)"] -->|NEFF| J["Run + Verify<br/>(hardware)"]
    end

    J --> K["Correctness + Metrics<br/>(latency, MFU)"]
