# Learnings

## Code Quality
- Don't rebuild derived maps in hot paths — cache on `id(obj)` at module level (per-call `inspect.getsource` = 50×+ slower). *(2026-04-23 10:00 ET)*
- **No `load_block`/`matmul_block`/`allocate_buffers` gadgets** — tile loops IR-positioned; bare `nl.ndarray`+`nisa.dma_copy`/`nc_matmul` at op fire depth. **`nl.ndarray` N-D explicit** `(P, num_p_tiles, F_contig)`; bare `128` (no `nl.par_dim`); prefer `nisa.dma_copy`. *(2026-04-26 11:00 ET)*
- **Loud failures only**: no silent raises, no `try/except` to adapt around malformed IR — reject bad input. A no-op-on-bad-input transform returning "success" is the same violation. **Single return per function** (user rejects branching returns). *(2026-06-01 22:00 ET)*
- **Transform legality = behavior/dep-order + ISA well-formedness ONLY; NEVER resource capacity** (user-locked): a dep-legal kernel that over-subscribes PSUM/SBUF is a VALID transform output — compile/HW profiling prunes it. DO reject malformed IR / dep violations / ISA-impossible ops (sub-128 `nc_matmul`); DON'T gate "won't fit in PSUM". *(2026-06-06 06:00 ET)*

## Architecture
- **`nkigym/__init__.py` lazy `__getattr__`**: eager re-exports drag heavy deps (`claude_agent_sdk`) into gym workers — keep `__init__` empty of compile/synthesis loads. **Editable pip install pins source path** across shared-venv worktrees: subprocess tests pick up installed path not cwd → `PYTHONPATH=<worktree>/nkigym/src`. *(2026-05-07 13:00 ET)*
- **Neuron allocator** Chaitin-Briggs+zone-aware: `address=` annotations skip `AddressRotation::run` → ~48pp MFU loss. *(2026-04-26 04:00 ET)*
- **Material-blocking**: reading PSUM w/ open material-blocking → `NCC_ISCH714`. Memset PSUM only when `PSUM_DTYPE=fp32`; transpose bf16 PSUM must NOT memset → `NCC_IBIR441`. HW mandates fp32 for `tensor_scalar.operand0/1`, `activation.scale`. *(2026-04-23 10:00 ET)*
- **Path B (own IR) > Path A (fork neuronx-cc)** — Path A ceiling = compiler IR (no online fusion/rotation/SW-pipeline/PSUM-bank addrs). **Decision: faithfully PORT exactly TVM's TensorIR schedule layer + `arith`** (NOT Relax/TE/topi); single target; win = online fusion + agent-legibility. *(2026-06-03 02:00 ET)*
- **arith substrate SHIPPED (`nkigym/ir/arith/`)**: faithful TVM `src/arith/` port. TVM dep **test-only** (`importorskip`), shipped `nkigym/` imports zero tvm. `can_prove_equal` can't fold `(f//c)*c+f%c→f` — use `detect_iter_map`. *(2026-06-03 12:00 ET)*
  - **Test-slimming = dedup/parametrize, PRESERVE EVERY ASSERTION** (user-locked): `@parametrize` near-identical tests, hoist helpers to `_fixtures.py`.
- **Repo scope**: `NKIOps` + `KernelIR` (`ir/ir.py`, wraps `DimensionAnalysis`+`KernelTree`) build + `render` to NKI; `.dump(cache_dir)` → markdown + tree PNG + `kernel.py`. *(2026-06-01 22:00 ET)*
- **NO trip-1 ForNodes anywhere**: a dim's iteration = factorization `(trip₀…trip_{k-1}, tensorize_size)`, product = extent; ForNode exists ONLY when trip > 1; innermost `tensorize_size` = ISA-leaf width. **Three layers**: `iter_var.dom`, ForNode trips, `BufferRegion (lo,width)`; invariant `Π(trip)×width==dom`. *(2026-06-01 02:00 ET)*
- **BlockNode IR** (`tree.py`, nx.DiGraph): payload union `BlockNode|ForNode|ISANode`. `BlockNode` (TVM `SBlock`) owns `iter_vars` (axis+`dom`+role), `iter_values` (affine loop_vars→iter_var), `reads`/`writes`, `alloc_buffers`. **Blocks mechanically emitted** (canonical_build = one-per-op; transforms = fixed recipe); a block carries ONE role per axis → differing role forces own block. `axis_map` = concrete→abstract (Split `d2`→op-axis `N`). *(2026-06-07 20:00 ET)*
- **SSA ops backend**: `f_nkigym` is SSA — `out = NKIOp()(src=…)`, allocate-and-return; `NKIAlloc`/`dst=`/authored memset GONE. Memset synthesized in `canonical_build` from `RMW_OPERANDS`. **Dtype** (`Buffer.physical_dtype`): PSUM accum ALWAYS fp32 (ONLY hardcode); else FIRST HBM input dtype. *(2026-06-01 22:00 ET)*
- **LCA buffer placement, re-derived after EVERY transform** (`place_buffers`): each `Buffer` on LCA BlockNode of all blocks touching it — the PSUM-hoist lever (compute_at co-locating drops LCA). `shared_hbm` pinned at root (else re-alloc → NaN). **Location enum `{shared_hbm, sbuf, psum}`** (literal IS var prefix; HBM 2D, SBUF/PSUM 3D). *(2026-06-21 16:00 ET)*
  - **Buffer alloc FORM + scope is MFU-CRITICAL, NOT inert** (MEASURED; CORRECTS "behaviorally inert"): k22 matched kernel_target's compute body yet ran **46.6% vs 90.7%** — all **buffer representation**. kernel_target uses a **list of separate `(128,1,512)` ndarrays alloc'd INSIDE each loop** (allocator rotates/double-buffers); we emit **ONE packed `(128,D,F)`** → loads serialize ([[nki-buffer-allocation-granularity]]). **Remaining diffs after fold-inlining** (full list + analysis in [[fused-rfactor-design-analysis]], unproven until byte-exact): list-of-tiles codegen emission; **store-sink LEGALITY blocker** (output block pinned at root); **psum N-compaction needs a MISSING region-rebase transform** (rewrite drain N-read onto enclosing `i_d2_0` w/o a new loop; `Split(d2)` adds inner `i_d2_1` → 4× sweep, MFU 27%) — M-compaction works. NB kernel_target has TWO memsets: psum-init at innermost `i_d1_1` (RFactor places right), `sbuf_prod`-init under N — psum-init under N → NaN.
- **Buffer-alloc granularity = the on-chip multi-buffer lever** (user-corrected, hand-tuning): encode **multi-buffer degree** as the **2nd dim** `(P,deg,F)` (atomic, pinned together), **number-of-tiles** as a **`list`** (liveness-scheduled independently); lifting tiles to a list while keeping only `deg` on the tensor is the MFU knob ([[nki-buffer-allocation-granularity]]). **All tensors stay 3D even at deg/tiles=1**; alloc at tightest enclosing scope. *(2026-06-18 18:00 ET)*
- **Producer-consumer deps = sidecar DAG** (`ir/dependency.py`, networkx; ISA-leaf nids ONLY — no ForNode/loop nodes). Tree stays block-structural — don't reparent loops to encode RAW. **ONE notion: same buffer + overlap region + RAW/WAW/WAR**, each edge tagged with its `tensor`; legality = leaf positions+spans. *(2026-06-03 02:00 ET)*
  - **Code-motion legality = SPAN-PROMOTION** (`fc3043b`; DELETED CARRY/COVER edge families + `skip_cover_loops` + `_check_no_reduction_axis_covered` + the `_carry_loops_of_leaf`/`_tiled_write_loops_of_leaf`/`_reads_independently_of_loop`/`_own_carry_loop_nids` heuristics): the shipped preorder-span backward-edge test (`first_backward_edge_for_insertion`) + ONE rule — promote an access's span from leaf-point to enclosing loop L's full span iff (a) L encloses it on the proposed tree, (b) L's var absent from the access's region offset (invariant), (c) `tensor` CARRIED across L. **CARRIED (role-BLIND)** = an `rmw` of the tensor invariant across L, enclosed by L, **AND NO plain-write init (write on a non-`RMW_OPERANDS`/non-`INPUT_OPERANDS` slot, invariant across L, enclosed by L) inside L** — an init inside L re-establishes the accumulator ⇒ L RE-INITS, not carries. Post-RFactor psum carried across `ki` (accumulation) NOT `ko` (memset re-zeros per-ko) though matmul rmw is invariant across BOTH → promote BOTH endpoints, memset-into-K ⇒ K-span backward ⇒ reject. RAW+init-domination+coverage all collapse to this ONE ordering test; replication stays the structural `_check_no_reduction_replicated`. See [[span-promotion-carried-across-ki-not-ko]], [[post-rfactor-matmul-k-parallel]]. *(2026-07-03)*
  - **Dep directions: freeze from ORIGINAL `ir.dependency`** — NEVER rebuild on MOVED tree (RAW→WAR flip hides the violation → NaN); eval via `first_backward_edge_for_insertion` on the PRE-move tree (positions analytic). Once CARRY deleted, `first_backward_edge(tree=moved.tree)` UNDER-rejects (76-case sweep: pure `_for_insertion` strictly catches more) — it is NOT a valid oracle ([[span-promotion-needs-frozen-directions]]).
- **Codegen direction: ir → codegen, never reverse.** Renderer takes IR primitives (tree/block nodes, `BufferRegion`s), not `KernelIR`. **loop_var naming**: dense `i_d{dim}_{N}`, N = ordinal among dim's ForNodes outer→inner. **Sibling order = dataflow**: splice `add_edge` at original child index. **Buffer decls emit before their first-use child** (`_alloc_emit_anchors` keys `anchor_child→[buf]`: first child of buffer's scope whose subtree touches it), interleaved like `kernel_0`, NOT bunched at node top. Depth-tightest *scope* (LCA, `ccc6a41`) and *position within scope* are separate levers. *(2026-06-24 11:00 ET)*
- **Transforms shipped: Split + Fuse + Reorder + CodeMotion + RFactor + SoftwarePipeline** (`nkigym/transforms/`). `Transform` base: `analyze(ir)→list[TransformOption]`, `apply(ir,option)→KernelIR` (deep-copy+mutate, re-checks → loud `TransformLegalityError`). **Split respects `MIN_TILE_SIZE`** (matmul K/M/N all 128) → M/K@128 unsplittable, N(512)→128. **`CodeMotion` (`13617ae`) MERGED ComputeAt+ReverseComputeAt** — they were one op (`is_reverse` was inert, `analyze`/`_legal_indices` byte-identical; producer-sink vs consumer-lift is EMERGENT from the dep graph). `CodeMotionOption(block_nid,target_loop_nid,index)`, no direction flag; `_move` lost `is_reverse`. **NO output-block guard** (was ComputeAt-only) → the k11→k12 OUTPUT-STORE sink is legal ([[k11-k12-store-sink]]); dep+span-promotion govern the store like any block. Module `_code_motion.py`→`code_motion.py`. Non-RFactor transforms can't merge/create blocks (count invariant). *(2026-07-03)*
- **TVM init = TWO-state model** (detail in `tvm_knowledge.md`): default WELDED (`block->init`, lowers PREDICATED `if reduce==min`); `decompose_reduction` extracts to a schedulable `SBlockRV`. **We're PERMANENTLY post-decompose** → never port init-field/predication/`decompose_reduction` (positional renderer can't emit `if`). *(2026-06-09 12:00 ET)*
- **Code-motion = SAME-PREFIX MERGE on DEPENDENT dims, NO region recomputation** (user-locked; replaces TVM `solve_iter_domains`/region-regen): **loop-nest MERGE** — prefix-match enforced **only on DEPENDENT dims** (loop vars the moved block binds), by `(dim,extent)`; **target loops the block does NOT bind become DUPLICATION loops** (replicate — correct for a pure producer/reload, rejected for ACCUMULATION). **`_move` VERBATIM-splices the subtree** (no normalize/region-regen) → byte-identical to hand kernel. **PREREQUISITE: Split/Reorder/Fuse to match the dependent prefix FIRST** (lhs_T K-loop Split to `ko(2)` before sinking); mismatch → reject loud. Duplication lets N-invariant lhs_T sink under matmul N = per-N reload (kernel_target's form). *(2026-06-22 00:00 ET)*
- **SoftwarePipeline SHIPPED (Tier B = stage assignment)** — faithful port of TVM `InjectSoftwarePipeline`. Knob = per-child-block `(stages, order)` on parent `BlockNode.annotations`; **version count DERIVED `use_stage−def_stage+1`** → `Buffer.versions`. Stageable = ALL direct loop children. Legality = TVM's 2 `ValidatePipelineBody` rules, NO capacity gate. **MEASURED: matmul 59.6%→83.4% = SW-pipeline ladder CEILING; fused-RFactor ladder LOWER at 46% until buffer-form solved.** *(2026-06-06 15:30 ET)*
- **RFactor: FUSED single-accumulator form SHIPPED** (`fbb1f5e`), replacing multi-slot terminal. **Split=role-preserving; RFactor=role-CHANGING** (flips factored loop ACC→PAR), legal ONLY via assoc+commutative reducer; `Split(K→ko,ki)∘RFactor(ko)`. `_emit_rmw` two-stage: per-tile PSUM (re-zeroed every `ko`), `ki` HW-accumulates, then `tensor_copy`+`tensor_tensor` fold carrying `ko` ACC, `sbuf_prod` memset once before `ko`. Must go **EARLY** (LATE → `KeyError`). **Why fused: NO `ko`-on-M term → `Split(M)` after RFactor stays machine-precision** (multi-slot baked `ko*m_tiles` onto psum M → diverged). Merged ladder canonical→kernel_target (`113d630`). *(2026-06-20 12:00 ET)*
  - **Coverage-guard barriers FIXED** (B1 `ed5f61a`; B2 superseded by prefix-merge above): legality **OVER-REJECTION** blocked fold-inlining — **no new transform, no dep-model/IR overhaul** (user explored+rejected "loop=block" IR). **B1 keys NIDs not loop_vars** (post-RFactor foreign-K & own-`ko` both named `i_d0_0`); golden-verdict gated. Fold-inlining BUILT + sim-clean; k0–k9 N-outermost sim-clean. See [[fused-rfactor-design-analysis]]. *(2026-06-21 20:00 ET)*
- **Memset = sibling block, NOT nested `matmul.init`** (`BlockNode.init` dropped; permanently post-`decompose_reduction`). **Init-domination is now SPAN-PROMOTION, not a `memset→K-loop` carry edge (edge deleted `fc3043b`)**: psum is carried across its reduction loop, so a memset sunk INTO that loop promotes to the loop's span → backward → rejected (re-zero per-iter → NaN). The init-breaks-carry clause is what lets a memset sink into `ko` (per-ko re-init, legal) while rejecting a sink into `ki` (would re-zero the live accumulation). *(2026-07-03)*
- **Loop-role placement (OP-LOCAL)**: `ForNode` role-neutral; role on `BlockNode.iter_var.role` (=`AXIS_ROLES[axis]`, default PARALLEL); same dim differs across ops. Split/Fuse role-blind; Reorder/compute_at consult. **3 roles**: PARALLEL, ACCUMULATION (reassociable — RFactor needs), SEQUENTIAL. **Reorder** = payload-swap adjacent parent-child ForNode pair; legal iff perfect-nest + no descendant SEQUENTIAL leaf on swap_dim. Bubble N outermost = chained swaps. *(2026-06-07 20:00 ET)*
- **MFU SOTA vs nkipy baselines** (`@baremetal_jit`→HLO→`neuronx-cc -O2`): matmul_lhsT_rhs 90.92% (>86.65%); matmul_lhs_rhs 84%; causal attn 13.20%. MAC = 4·SEQ²·D (attn), `K·M·N` (matmul). **`throttle_avg_util_limit` NOT HW ceiling**; gap = `tensor_engine_active_time_percent`. *(2026-06-05 12:00 ET)*
- **ISA**: `dma_transpose` HBM→SBUF AND SBUF→SBUF, matching dtypes (`dma_copy` tolerates mismatch). `nc_transpose(dst=psum)` IS `nc_matmul(moving=identity, is_transpose=True)`, `data=` NOT `src=`. Tile limits: Load/Store/transpose P=128; nc_matmul K/M=128, N=512. `nisa.activation_reduce` free-axis cap **16384/partition**, `reduce_cmd=reset_reduce`, `dst.shape==data.shape`. `tensor_scalar.operand0` broadcasts FREE. **NKIOp = ISA 1:1, no invented kwargs.** `@nki.jit` nesting fails → `BaremetalExecutor.benchmark`. *(2026-05-05 20:00 ET)*

## Verification
- `NCC_ISCH714` also fires on aliased `address=` RAW. *(2026-04-23 10:00 ET)*
- **Byte-exact transform verification**: gate = `render(apply(before_IR)) == <hand kernel>`, NOT "roughly the same" (user: "same means same"). Oracle (`_ladder_compare.py`) **AST-canonical** (positional→keyword, sorts kwargs, distributes affine). **The hand fixture MUST encode the SPEC shape, NOT the transform's own captured render** — else "byte-exact ✅" means "matches what it emitted", not "matches spec". Author the fixture by hand. **Decl-hoist is TOP-LEVEL ONLY**: oracle hoists only function-level `nl.ndarray` decls, NOT in-loop → **in-loop decl position IS significant** (guards `test_apply_byte_exact`); top-level position normalized away (gate with a `hoist_decls=False` variant). *(2026-06-24 11:00 ET)*
- **Goal = REPRODUCE `kernel_target` exactly, not "equivalent"** (user-locked: "no cheating, don't act smart"): when MFU is flat, **literal-diff and close EVERY difference** — don't rationalize a delta as a separate optimization. **"Only N blockers left" is unprovable short of byte-exact repro** — enumerate verified diffs, don't claim the set is closed. *(2026-06-21 16:00 ET)*
- **Numeric sim is a WEAKER oracle than dependency-order** — legality = dep-order preservation, NOT numeric luck. memset-after-matmul / drain-sunk-into-K sim "correct" by last-write-wins yet violate producer/consumer order + break HW; a rejecting check is RIGHT even when sim passes. **Behavior-preserving legality refactor → GOLDEN-VERDICT gate, not the suite** (a topological swap can over-permit `illegal→ok` while suite+sim stay GREEN). *(2026-06-04 16:00 ET)*
- **Transforms COMPOSABLE + always-correct-when-legal** (user-locked): EVERY intermediate kernel in a chain MUST be correct — a wrong one (k16/k17 NaN) means **the legality check failed to reject it; fix the CHECK, not the ladder** ("the solution is not 'let's not do this ladder'"). Verify each rung before the next. *(2026-06-21 20:00 ET)*
- **FP32 validation contract**: CPU-sim runs fp32 (inputs cast, SBUF/PSUM→`nl.float32`, fp32 golden); HW keeps original. End-to-end `atol=rtol=5e-3` (`assert_allclose` raises+halts); unit — elementwise `1e-6`, matmul `1e-5`, reduce `1e-4`. **`simulate_fp32` (`nkigym.synthesis`) sims bare `@nki.jit` reference kernels vs numpy golden, not just rendered IR** (`examples/manual_transforms.py` auto-discovers each `kernel_<id>`). *(2026-06-23 12:00 ET)*

## Workflow
- **Target-state pipeline**: programmatic front-end; agentic rewrite proposer; codegen; profile; offline priors. NO source-repair. User prefers named factor fields; rejects per-primitive struct abstractions. **Design doc = source of truth**: massive removals over compat shims. **New spec supersedes → DELETE the old** (user) — fold surviving bits in; first check it's not the SOLE record of *shipped* code. *(2026-06-07 20:00 ET)*
- **Subagents inherit the main-session model** — never pass a `model` override. **Verify their reports yourself** (run `git log`/`ls`/`pytest`; they claim "22 passed" with no files changed); a BLOCKED status with credible root-cause is often right. Controller owns ALL remote runs + nid-scouts + fixtures; subagents edit+commit only. *(2026-05-31 06:00 ET)*
- **On TVM/source questions: VERIFY from source, don't reason between fragments** (user: "you keep walking back on yourself"). For a falsifiable claim about external code, dispatch parallel read-only agents each owning ONE question with file:line evidence, synthesize ONCE. "source-traced" ≠ "test-proven"; a negative claim demands exhausting the source. *(2026-06-08 23:00 ET)*
- **Two-stage review (spec→quality) catches bugs unit tests miss** — e.g. a **plan-invented ISA kwarg** (`in0/in1` vs real `nisa.tensor_tensor(data1,data2)`) `TypeError`s at render but ClassVar/`_run` tests never render. *(2026-06-09 04:00 ET)*
- **Bound every tool call** — oversized batched reads / wide parallel Bash in a long session → repeated `API Error: Internal server error` (user-corrected twice). Targeted `grep|head` + chunked Edits. *(2026-06-05 08:00 ET)*
- **Cache hygiene**: examples delete `CACHE_DIR` at start. `mmdc` needs `--puppeteerConfigFile '{"args":["--no-sandbox"]}'` + delete `*.puppeteer.json` after; `*.png` gitignored → per-path `!…/*.png`. **pre-commit black/isort/autoflake reformat+abort** (black `line-length=120`) — re-stage, retry. *(2026-05-30 17:00 ET)*
- **Fresh-box: run `./install.sh`** (MANUAL prep — transport does NOT auto-run it; does NOT build TVM): venv + Neuron SDK + **`aws-neuron/nkipy` editable** (`$NKIPY_SRC`=`$HOME/src/nkipy`; nkigym hard-imports `nkipy.runtime.BaremetalExecutor`) + `dump()` host tools. *(2026-06-05 23:00 ET)*
- **"One fix per sampling run"**: one diagnosis+fix, report, stop (multi-fix reverted). **Strict scope**: exploratory/diagnostic request → STOP, don't extend into a fix. **User abort ("restore"/"restart")** = `git reset --hard origin/<branch>` + wipe cache; confirm+show target SHA first. *(2026-04-26 03:00 ET)*
- **Hand-writing kernel.py anti-pattern**: go through IR+codegen; hand kernels = correctness reference only. **One example file per workload**. **Repro/debug = flat hand-stepped env trace** (user-corrected): `s=env.reset(); s=env.step(s,(Transform(),Option(literal_nids)))`; `build_initial_ir` deterministic → nids STABLE, hardcode. **Iterate IN the example file rendered to its FIXED inspectable cache — NOT throwaway probe scripts** (user, emphatic): `examples/kernel_transforms.py` → `--cache /home/weittang/workplace/cache/transforms` so the user sees every `kernel_N.py`. *(2026-06-22 02:00 ET)*
- **Hand-tuning a kernel = inline to hot path, frozen reference + scratch copy** (`profile_matmul_lhsT_rhs_hand.py`): kernel as SOURCE STRING; FROZEN `KERNEL_SOURCE` beside editable `SCRATCH_SOURCE`, profiled side-by-side via CPU-sim+MFU. **Inlining rules**: constant-fold if/else to hot branch; inline ALL helpers; FOLD index aliases (`out_tile=i*4+j`) into subscripts; canonical loop-vars `i_d{dim}_{ordinal}`. *(2026-06-17 19:00 ET)*
- **Kaizen Desktop (run-on-trn2; `ssh_host.sh` preferred now, Kaizen broken)**: env IS the `--image` (py312 conda bakes in NKI/neuronxcc/torch). **ONLY `$HOME` survives** (S3-backed); apt/global-npm ephemeral → venv+Node under `$HOME`, `--system-site-packages` for baked-in NKI. *(2026-06-06 03:00 ET)*
- **Dev box has NO Python env** — `nki`/`neuronx-cc` on PyPI are decoy stubs, real `kernel-env` lives ONLY on remote; NEVER assume local `pytest`. **Run unit tests via `transport/ssh_host.sh` to gym-1** — `remote_pytest.sh` now STALLS at "Resolving desktop default" (Kaizen broken); `ssh_host.sh` activates venv + sets `PYTHONPATH=.:nkigym/src:autotune/src`, **requires `--cache`** even for pytest (scratch dir), and **`--cmd` needs a `.py` token** (bare `test/` dir rejected → enumerate files). **Verify "new" failures vs parent commit** — pre-existing set is **7** (transform-legality/code-motion, e.g. `target_loop_nid not in tree`), identical at parent. Desktop s5cmd-synced NOT a git remote; no local `black` → get `black --diff` from remote. *(2026-06-24 11:00 ET)*
- **`transport/kaizen.sh`** (`3db0609`; Kaizen fallback, now broken): `--name <d> --cmd "python X.py" --cache /abs`. `connect --cmd` SYNCHRONOUS → exit code (137=OOM); **`$HOME`→S3 export LAGS ~60s + TRICKLES** → `.transport_manifest` sentinel-last + re-sync (the lag `ssh_host.sh` avoids). *(2026-06-10 14:00 ET)*
- **`transport/ssh_host.sh` SHIPPED — prefer over Kaizen** (`391305b`; user: "stop using kaizen, use SSH to gym-1"): rsync sibling of `kaizen.sh` for ALWAYS-ON gym-* lab Trn2 boxes (user=ubuntu); `--host --cmd --cache`; SYNCHRONOUS bidirectional rsync → NO S3 lag/polling. **Why: Kaizen `nrt_init` fails `err 6` = host-driver↔image ABI mismatch** ([[kaizen-nrt-init-driver-mismatch]]); gym-1's driver `2.28.0.0` inits cleanly. *(2026-06-19 15:00 ET)*
- **Autotune backend SHIPPED (`dev_1`)**: `runner/` stripped to on-Trn2 in-process (SSH-coordinator `remote.py`/`worker.py` DELETED → `driver.run_pipeline`+`api.profile()`); drivers REQUIRE `--cache`. `nkigym` hard-imports `nki.isa` at op-load → whole driver on box. *(2026-06-05 12:00 ET)*
- **Matmul MFU ladder** (2048³ bf16 Trn2; gaps = **operand-PREFETCH bubble**). MEASURED: **~16%** (inner-K single-buf) → **59%** (PSUM-tiled+hoisted) → **83.7%** (accum double-buf `(128,2,N)`+`%2`) → **89.5%** (loads SUNK+double-buf) → **90.6%** (+rfactor) → **90.9%** (scheduler-off flags `linear-scan-allocation`/`instruction-scheduling=false`). *(2026-06-06 15:00 ET)*

*Last updated: 2026-06-24 11:00 ET*
