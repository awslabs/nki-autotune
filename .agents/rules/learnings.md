# Learnings

## Code Quality
- **Hot paths**: cache derived tree topology/access facts by `KernelTree` in `WeakKeyDictionary`, reuse shared analysis context across options, and invalidate mutation-sensitive caches immediately after a rewrite. *(2026-08-05 14:00 ET)*
- **No `load_block`/`matmul_block`/`allocate_buffers` gadgets** — tile loops IR-positioned; bare `nl.ndarray`+`nisa.dma_copy`/`nc_matmul` at op fire depth. **`nl.ndarray` N-D explicit** `(P, num_p_tiles, F_contig)`, bare `128` (no `nl.par_dim`). *(2026-04-26 11:00 ET)*
- **Loud failures only**: no silent raises, no `try/except` to adapt around malformed IR — reject bad input. A no-op-on-bad-input transform returning "success" is the same violation. **Single return per function** (user rejects branching returns). *(2026-06-01 22:00 ET)*
- **Transform legality = behavior/dep-order + ISA well-formedness ONLY; NEVER resource capacity** (user-locked): a dep-legal kernel that over-subscribes PSUM/SBUF is a VALID output — compile/HW profiling prunes it (gym-1 CONFIRMED). DO reject malformed IR / dep violations / ISA-impossible ops; DON'T gate "won't fit in PSUM". *(2026-07-03 10:00 ET)*

## Architecture
- **`nkigym/__init__.py` lazy `__getattr__`**: eager re-exports drag heavy deps into gym workers — keep `__init__` empty of compile/synthesis loads. **Editable pip install pins source path** across shared-venv worktrees: subprocess tests pick up installed path not cwd → `PYTHONPATH=<worktree>/nkigym/src`. *(2026-05-07 13:00 ET)*
- **Neuron allocator** Chaitin-Briggs+zone-aware: `address=` annotations skip `AddressRotation::run` → ~48pp MFU loss. *(2026-04-26 04:00 ET)*
- **Material-blocking**: reading PSUM w/ open material-blocking → `NCC_ISCH714` (also on aliased `address=` RAW). Memset PSUM only when `PSUM_DTYPE=fp32`; transpose bf16 PSUM must NOT memset → `NCC_IBIR441`. HW mandates fp32 for `tensor_scalar.operand0/1`, `activation.scale`. *(2026-04-23 10:00 ET)*
- **Path B (own IR) > Path A (fork neuronx-cc)** — Path A ceiling = compiler IR (no online fusion/rotation/SW-pipeline/PSUM-bank addrs). **Decision: PORT TVM's TensorIR schedule layer + `arith`**. *(2026-06-03 02:00 ET)*
- **arith substrate SHIPPED (`nkigym/ir/arith/`)**: faithful TVM `src/arith/` port; shipped code imports zero TVM. Optional TVM parity oracles remain removed because the supported install never carried TVM. `can_prove_equal` can't fold `(f//c)*c+f%c→f` — use `detect_iter_map`. *(2026-08-03 16:00 ET)*
- **Repo scope**: `NKIOps` + `KernelIR` build and render NKI. `KernelIR.dump()` writes only `envelope.md` and formatted `kernel.py`; the user removed the Mermaid/PNG backend and Node/browser dependencies. *(2026-07-17 13:00 ET)*
- **NO trip-1 ForNodes anywhere**: a dim's iteration = factorization `(trip₀…trip_{k-1}, tensorize_size)`; ForNode ONLY when trip>1; innermost `tensorize_size` = ISA-leaf width. **Three layers**: `iter_var.dom`, ForNode trips, `BufferRegion (lo,width)`; `Π(trip)×width==dom`. *(2026-06-01 02:00 ET)*
- **BlockNode IR** (`tree.py`, nx.DiGraph): payload union `BlockNode|ForNode|ISANode`. `BlockNode` (TVM `SBlock`) owns `iter_vars` (axis+`dom`+role), `iter_values`, `reads`/`writes`, `alloc_buffers`. **Blocks mechanically emitted** one-per-op; ONE role per axis → differing role forces own block. *(2026-06-07 20:00 ET)*
- **SSA ops backend**: `f_nkigym` is SSA — `out = NKIOp()(src=…)`, allocate-and-return; memset synthesized in `canonical_build` from `RMW_OPERANDS`. **Dtype** (`Buffer.physical_dtype`): PSUM accum ALWAYS fp32 (ONLY hardcode); else 1st HBM input. *(2026-06-01 22:00 ET)*
- **Buffer placement and compaction are separate actions**: `BufferPlacement` moves only one selected on-chip declaration to its lifetime-safe LCA; `shared_hbm` stays at root. `BufferCompaction` only shrinks one selected logical shape and normalizes its regions, preserving `list_len`. CodeMotion/RFactor do neither. *(2026-08-03 16:00 ET)*
  - **Buffer alloc FORM is MFU-CRITICAL, NOT inert** (MEASURED): k22 matched the manual endpoint's compute body yet ran **46.6% vs 90.7%** — pure buffer form (manual endpoint: list of `(128,1,512)` tiles IN-loop → double-buffered; ours: ONE packed `(128,D,F)` → serialized loads). [[nki-buffer-allocation-granularity]].
  - **Free-axis shrink is a CodeMotion→BufferCompaction composition property**: it shrinks to 512 only after the offset becomes enclosing anchor `i_d2_0`, not from `Split(d2)` alone.
- **Buffer-alloc granularity = the on-chip multi-buffer lever** (user-corrected): **multi-buffer degree** = 2nd dim `(P,deg,F)` (atomic, pinned); **number-of-tiles** = a **`list`** (liveness-scheduled) — lifting tiles to a list is the MFU knob ([[nki-buffer-allocation-granularity]]). All tensors 3D even at deg/tiles=1; alloc at tightest scope. **Target model: tile axis factorizes `[P,T,F] → [[P,a,F]]×b` for ANY `a·b=T`** (`Buffer.list_len`=`b`); **codegen emits UNIFORM list even for `b=1`** (perf-neutral). **`BufferLayout` CONSERVES tiles — re-factorizes `a·b=T`, never creates** (`[P,2,F]×8` double-buffer = SoftwarePipeline's `versions`). *(2026-07-06 16:00 ET)*
  - **Partition axis P = MAX 128, NOT always-128** (user-corrected): per-op min/max caps ARE already fields (`MIN_TILE_SIZE`/`MAX_TILE_SIZE: ClassVar[dict[str,int|None]]`). But P is PINNED to exactly 128 by `MIN["P"]=128` AND a redundant `PARTITION_DIM` (`tree.py:35`, asserts `leading%128==0`) → P<128 UNREPRESENTABLE today = separate future plan, NOT a missing field. **`BufferLayout` P-agnostic** — factorizes tile axis T, passes `0:P` through.
- **Producer-consumer deps = sidecar DAG** (`ir/dependency.py`, networkx; ISA-leaf nids only). The tree stays block-structural. Edges are overlapping same-buffer RAW/WAW/WAR tagged by `tensor`; static scheduling uses `must_precede`, while CodeMotion evaluates positions and promoted spans. *(2026-08-07 01:00 ET)*
  - **Code-motion legality = span-promotion plus reset/stage preservation**: carry invariant RMW unless a plain-write init breaks it; reject reset-frequency or software-pipeline stage-skew changes, and invariant full-buffer consumers replicated inside sliced producer loops.
  - **Dep directions: freeze from ORIGINAL `ir.dependency`** — NEVER rebuild on MOVED tree (RAW→WAR flip hides the violation → NaN); eval via `first_backward_edge_for_insertion` on the PRE-move tree ([[span-promotion-needs-frozen-directions]]).
- **Codegen direction: ir → codegen, never reverse.** Renderer takes IR primitives (tree/block nodes, `BufferRegion`s), not `KernelIR`. **loop_var naming**: dense `i_d{dim}_{N}` (N = ordinal outer→inner). **Sibling order = dataflow**. **Buffer decls emit anchored to first-use child** (`_alloc_emit_anchors`, `body.py`; NOT `alloc_buffers` list order); *scope* (LCA) and *position* = separate levers. **Renderer output IS canonical style** — kwarg ISA + slice indexing. *(2026-06-24 11:00 ET)*
- **Transform API/registry**: every public transform directly defines one typed `analyze(ir)` that exposes legal options and one typed `apply(ir, option)` that re-checks, deep-copies, and fails loudly. Ordered `PUBLIC_TRANSFORM_TYPES`/`public_transforms()` feeds search and rollout and must match the source-discovered public inventory. OnlineFusion uses operation contracts and one-stage incremental prefix/completion states for arbitrary contract-proven chain lengths; useful intermediates remain independently valid. *(2026-08-07 01:00 ET)*
- **Self-evolution/search**: agentic tuning explores a measured branching tree with 1–3 sequential legal actions, profiles every child, and reuses render-identical evaluations. `$self-evolve` runs the standalone tests as a read-only preflight and aborts without repairs on any failure. On a healthy checkout it requires an explicit kernel-library workload, host, and positive round count; stores durable artifacts outside the repository; and edits the same checkout without creating another worktree. Temporary Git indexes preserve staged and unstaged state; acceptance records the exact checked tree without committing or rewriting the branch or index. *(2026-08-06 00:17 ET)*
- **TVM init = TWO-state model** (detail in `tvm_knowledge.md`): default WELDED (PREDICATED `if reduce==min`); `decompose_reduction` extracts a schedulable `SBlockRV`. **We're PERMANENTLY post-decompose** → never port init-field/predication/`decompose_reduction`. *(2026-06-09 12:00 ET)*
- **CodeMotion = same-prefix merge on dependent dimensions** (replaces TVM `solve_iter_domains`): match ordered `(dim, extent)` pairs, never generated `loop_var` names; retain NID identity for shared ambient loops and execution-scope checks; rebind removed-loop uses to target variables and normalize only the moved block. Non-bound target loops are duplication loops (valid for producer/reload, rejected for accumulation). Split/Reorder/Fuse must first make the dependent prefix match. *(2026-07-18 01:00 ET)*
- **SoftwarePipeline** assigns stage labels in fixed source-child order and derives `Buffer.versions = use_stage−def_stage+1`; ordering is not a second scheduling choice. Small bodies use exhaustive labelings; large bodies enumerate contiguous two/three-stage partitions in $O(n^2)$. No capacity gate. *(2026-08-03 16:00 ET)*
- **Split/Fuse/RFactor atomic boundary**: Split and Fuse replace exactly two adjacent factors/entries per action. Split never partitions a slot reduction; public RFactor owns the coordinated `ko/ki` split plus reset/copy/fold as one semantic atom. RFactor derives axes/domains from source, does not reorder loops, and does not place or compact buffers. *(2026-08-03 16:00 ET)*
- **BufferLayout** conserves logical tiles and sets `list_len`; it commutes with SoftwarePipeline. A versioned list has `tiles_per_list * versions` physical slots, and renderer indexing selects both the list-local tile and pipeline slot. `analyze` enumerates every divisor of T. *(2026-07-29 11:00 ET)*
  - **Expression rendering preserves AST precedence**: compound substitutions need parentheses in `Mul`, `FloorDiv`, and `Mod`; otherwise BufferLayout, pipeline, and RFactor slot indices silently change. Bare-`Var` tests miss this, so whole rollouts must cover composed affine expressions. *(2026-08-07 01:00 ET)*
  - **Legality checks key NIDs, NOT loop_vars** (`ed5f61a`; post-RFactor foreign-K & own-`ko` both named `i_d0_0` → loop_var keys alias-collide → over-rejection blocked fold-inlining). Fixed with no new transform / no IR overhaul. See [[fused-rfactor-design-analysis]].
- **Memset = sibling block, NOT nested `matmul.init`** (`BlockNode.init` dropped; permanently post-`decompose_reduction`). **memset-into-K = plain producer-before-consumer** (re-zeros a live partial-sum → NaN); subsumed by span-promotion. *(2026-07-03 10:00 ET)*
- **Loop-role placement (OP-LOCAL)**: `ForNode` is role-neutral; roles live on `BlockNode.iter_var` and may differ by op. Roles are PARALLEL, ACCUMULATION, and SEQUENTIAL. Reorder swaps one adjacent perfect-nest pair, rejects descendant SEQUENTIAL axes, and requires internal dependency endpoints to agree on whether each swapped loop indexes their tensor. *(2026-07-17 13:00 ET)*
- **Kernel-library HW gate**: on the configured host, final generated lhsT matmul must reach 90% MFU, RMSNorm+matmul 85% and within 3 points of lhs matmul, and attention 44%. The test owns endpoint selection, scheduler-disabled compiler flags, and thresholds. Recent measurements are ~90.6%, ~86.8%, and ~45.2%. MAC = 4·SEQ²·D (attention), `K·M·N`. *(2026-08-03 16:00 ET)*
- **ISA / transpose**: `dma_transpose` supports HBM→SBUF and SBUF→SBUF with matching dtypes. `NKITranspose` emits `nisa.nc_transpose(dst, data=...)` (not `src=`), SBUF→PSUM, matching dtype, max 128x128; PSUM location does not itself force fp32. Layout tuning exposes only `InsertTransposePair`, `CancelTransposePair`, and operation-specific `TransposeThrough{Load,Matmul,TensorCopy}` commutes; no macros or compatibility wrappers. Pair insertion names an exact producer-consumer edge, and every intermediate preserves behavior. Canonical matmul uses the actual extent below N's 512 maximum. Matmul K/M≤128, N≤512; `activation_reduce` free cap 16384/partition; `tensor_scalar.operand0` broadcasts free. **NKIOp = ISA 1:1.** *(2026-07-26 11:00 ET)*

## Verification
- **`/report` = read the PERIOD's ACTUAL DIFFS, not commit-msgs/specs/stale `learnings.md`** (user corrected 3× in one run): a spec's root-cause section reported a bug FIXED that ISN'T; called the USER's hand `manual_transforms.py` "machine output"; re-reported a ~2wk-old batch as the delta. `git diff origin/<branch>..HEAD` the window, don't narrate from memory. *(2026-07-14 19:00 ET)*
- **Canonical acceptance = five pytest gates**: repository structure/API/dependencies, 20-shape random rollout, transform atomicity/genericity, fixed-ladder MFU, and stochastic agentic tuning within 1 MFU point of historical best. Workflow code adds only exact-tree/process/artifact checks. `test/test_repository_structure.py` is authoritative: at most 20 public transforms and 4,000 IR code lines; each direct `transforms/*.py` except `__init__.py`/`base.py` defines one public transform, directly implements typed synchronous `analyze`/`apply`, and is under 1,000 code lines. Shared code belongs only in flat `transforms/helper/`, with no transforms and under 1,000 total code lines. LOC excludes blanks/comments/docstrings; moving helpers elsewhere is cheating. Dependencies remain `nkigym`-only, `kernel_library`→`nkigym`, test→`nkigym`; skill support stays private. Do not restore brittle shape tests or branch every rollout action. *(2026-08-06 03:00 ET)*
- **Goal = REPRODUCE the manual-ladder endpoint exactly, not "equivalent"** (user-locked: "no cheating, don't act smart"): when MFU is flat, **literal-diff and close EVERY difference** — don't rationalize a delta as a separate optimization. *(2026-07-24 01:00 ET)*
- **Numeric sim is a WEAKER oracle than dependency-order** — legality = dep-order preservation, NOT numeric luck. memset-into-K / drain-sunk-into-K sim "correct" by last-write-wins yet violate producer/consumer order + break HW; rejecting is RIGHT even when sim passes. **Behavior-preserving legality refactor → GOLDEN-VERDICT gate** (a swap can over-permit `illegal→ok` while suite+sim stay GREEN). *(2026-06-04 16:00 ET)*
- **Each action is one semantic transform and every intermediate must be correct** (user-locked). Synchronized invariant repair intrinsic to that rewrite is allowed; hidden scheduling choices, composites, aliases/presets, convenience wrappers, no-ops, and workload-specific implementations are not. Agentic evaluation must source-audit every public transform for both atomicity and genericity. Rollouts record random 63-bit seeds and validate every selected state. *(2026-08-03 16:00 ET)*
- **FP32 validation contract**: CPU simulation casts inputs and SBUF/PSUM to fp32 and compares with a NumPy golden; hardware keeps original dtypes. NumPy front-end validation uses `5e-3/5e-3`; workload `atol/rtol` are matmul `1e-3/1e-3`, RMSNorm+matmul `1e-3/2e-2`, and attention `1e-5/2e-2`. `simulate_fp32` runs bare `@nki.jit`; hardware `profile()` measures compile + MFU/latency only and never validates numerics. *(2026-08-07 01:00 ET)*

## Workflow
- **Front ends**: `compile_numpy_to_nkigym` locally traces supported NumPy into an expression DAG, lowers once to `NKIOp`, builds IR, and validates fp32; no model, prompt, search, or retry. `$self-evolve` loads the selected kernel-library `Workload`; never redefine its graph/specs. Current code/docs are authoritative; deleted historical plans/diagrams stay deleted unless requested. *(2026-08-05 14:00 ET)*
- **Subagents inherit the main-session model** — never pass a `model` override. **Verify their reports yourself** (they claim "22 passed" with no files changed); a BLOCKED status with credible root-cause is often right. Controller owns ALL remote runs + fixtures; subagents edit+commit. **Hand-written GROUND-TRUTH fixtures are the USER's** (machine-authored intermediates reverted `81a5760`). *(2026-07-08 00:00 ET)*
- **On TVM/source questions (and BULK doc/artifact classification): VERIFY from source, don't reason between fragments** (user: "you keep walking back on yourself"). For a falsifiable claim, dispatch parallel read-only agents each owning ONE question with file:line evidence. "source-traced" ≠ "proven". *(2026-06-08 23:00 ET)*
- **Research files**: source locations and expert kernels are indexed in `references.md`; detailed TVM TIRx findings and evidence levels live in `tvm_knowledge.md`. *(2026-07-17 13:00 ET)*
- **Two-stage review (spec→quality) catches bugs unit tests miss** — e.g. a **plan-invented ISA kwarg** (`in0/in1` vs real `nisa.tensor_tensor(data1,data2)`) `TypeError`s at render but ClassVar/`_run` tests never render. Answer a finding by VERIFYING from source — "must test X" can be a structural impossibility (prove, don't fake). *(2026-06-09 04:00 ET)*
- **Bound every tool call** — oversized batched reads / wide parallel Bash → repeated `API Error` (user-corrected twice). Targeted `grep|head`, chunked Edits. *(2026-06-05 08:00 ET)*
- **Setup**: `./install.sh --host <host>...` creates `~/venvs/kernel-env` locally/on every remote, installing local `nkigym`/Black/pytest and remote `nkigym` from binary wheels. It does not provision drivers/tools or activate the shell; NKI 0.5/neuronx-cc 2.26 remain required. *(2026-08-06 03:00 ET)*
- **`/git-commit` skill: NO Claude co-author line** (overrides the global default; auto-amended it away twice). **Squash unpushed commits without interactive rebase** via `git commit-tree` chained on each group's END-tree onto `origin/<branch>` → final tree byte-identical to old HEAD (verify empty diff), then move the ref. *(2026-07-14 19:00 ET)*
- **Scope discipline**: read-only investigations stop before edits. A debugging session only tests and repairs the current checkout until all reproducible failures are cleared. `$self-evolve` only develops a healthy checkout, aborts and alerts on failed health checks or `mode=repair`, and never starts debugging itself. Each refinement round makes one measured semantic change in that checkout. If the user rejects a run, delete only its external artifacts and do not revert source changes. *(2026-08-06 00:17 ET)*
- **Kernel library owns standalone fixed ladders** (user-locked): each exact `(workload, shape)` has one flat module whose singular `Workload` owns input specs, NumPy/NKIGym functions, seeded FP32 input generation, tolerances, literal ladder, historical best, and manual `reference_mfu`; the module owns dump/check/profile and the registry maps tuples. Generated NKI/hand kernels remain untracked; `examples/` stays deleted. Random rollout samples five shapes/family, logs replay seeds, follows one 500-action path/shape (10,020 states), renders all states, then batch-simulates fp32 across `gym-cpu-1..4`; no fan-out. *(2026-08-07 01:00 ET)*
- **Local tooling**: use `~/venvs/kernel-env`; root `pyproject.toml` owns Pyright, pytest, Black, and isort. CPU simulation validates correctness; hardware profiling only performance. *(2026-07-31 23:00 ET)*
- **Profiling lives in `nkigym.profile`**: public `profile(host, kernel, func_name, input_specs, cache_dir, ...) -> (mfu_percent, latency_ms)` sends one standalone kernel to the SSH worker, compiles/profiles on Trn2, and stores artifacts. Kernel latency comes from Neuron Explorer `total_time`; `ProfileResult.elapsed_s` is unused remote compile+profile wall time, not latency. Arbitrary remote Python execution is unsupported. *(2026-08-03 16:00 ET)*
- **Matmul MFU ladder** (2048³ bf16 Trn2; gaps = **operand-PREFETCH bubble**): **~16%** (inner-K single-buf) → **59%** (PSUM-tiled+hoisted) → **83.7%** (accum double-buf `(128,2,N)`+`%2`) → **90.9%** (loads SUNK + rfactor + scheduler-off). *(2026-06-06 15:00 ET)*

*Last updated: 2026-08-07 01:04 ET*
